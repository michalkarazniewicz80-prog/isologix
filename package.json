{
  "name": "isologix",
  "version": "1.0.0",
  "description": "Isologix project with Netlify functions and Supabase authentication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "netlify dev",
    "build": "echo \"No build command specified\" && exit 0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.7.0",
    "node-fetch": "^2.6.7"
  }
}

