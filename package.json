{
  "name": "isologix",
  "version": "1.0.0",
  "description": "Isologix project with Netlify functions and Supabase authentication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "netlify dev",  // Add this if you're using Netlify CLI for local development
    "build": "echo \"No build command specified\" && exit 0"  // Custom build command if necessary
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.7.0",  // Supabase client library
    "node-fetch": "^2.6.7"              // If using Node.js version < 18
  }
}
